extends layout 
block content 
    h1 #{book.title}
    h2 Book Title: #{book.title}
    h2 Book Author: #{book.author}
    h2 Genre: #{book.genre}
    h2 Year Published: #{book.year}
    p
        a(class="button", href=`/books/${book.id}/edit`) Edit this book
    p
        a(class='button', href="/") Back 
        