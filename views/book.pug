extends layout 
block content 
    h1 #{book.title}
    h2 #{book.author}
        p #{book.genre}
        p #{book.year}
    p
        a(class="button", href=`/books/${book.id}/edit`) Edit this book
    p
        a(class='button', href="/") Back 
        